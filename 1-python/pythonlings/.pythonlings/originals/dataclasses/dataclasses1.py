# Exercise: Dataclasses 1
# I AM NOT DONE
#
# Make `Point` a dataclass with two integer fields, `x` and `y`.
# Add the @dataclass decorator and the two typed field declarations.

from dataclasses import dataclass


class Point:
    pass


origin = Point(0, 0)
