# Exercise: Variables 7
# I AM NOT DONE
#
# Swap the values of `a` and `b` using a single Pythonic swap statement,
# so that after the swap a == 20 and b == 10.
# Replace the ??? line with the swap.

a = 10
b = 20
???
