# Exercise: Functional 2
# I AM NOT DONE
#
# Assign a lambda to `absolute` that takes one number and returns its
# absolute value — WITHOUT using the built-in `abs()` function.
# Use a conditional expression:  x if x >= 0 else -x

absolute = ???
