# Exercise: Classes 1
# I AM NOT DONE
#
# Define a class called `Cat`.
# The class body can be a single `pass` statement — it just needs to exist.
# Then create an instance of it and assign it to `my_cat`.

# Replace ??? with the class body (a single `pass`).
class Cat:
    ???


my_cat = Cat()
