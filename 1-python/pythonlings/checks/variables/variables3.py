assert count == 7 and isinstance(count, int), "count should be int 7"
assert ratio == 0.5 and isinstance(ratio, float), "ratio should be float 0.5"
assert is_ready is True, "is_ready should be True"
print("variables3 ✓")
