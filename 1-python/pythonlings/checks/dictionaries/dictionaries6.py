assert timeout == 60, f"timeout should be 60 (from config), got {timeout!r}"
assert retries == 3, f"retries should be 3 (default), got {retries!r}"
assert verbose == False, f"verbose should be False (default), got {verbose!r}"
print("dictionaries6 ✓")
