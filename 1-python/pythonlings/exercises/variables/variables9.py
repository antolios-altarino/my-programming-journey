# Exercise: Variables 9
# I AM NOT DONPS constant TAX_RATE equal to 0.2 (20 %), then compute
# `total_price` as the price after tax: price * (1 + TAX_RATE).
# Replace the ??? placeholders with the correct values/expressions.

TAX_RATE = 0.2
price = 50
total_price = price * (1 + TAX_RATE)
print(total_price)
