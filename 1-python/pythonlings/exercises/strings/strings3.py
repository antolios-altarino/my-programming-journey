# Exercise: Strings 3
#
# Using indexing on `word`, assign its first character to `first_char`
# and its last character to `last_char`.

word = "python"
first_char = word[0]
last_char = word[-1]
print(last_char)
